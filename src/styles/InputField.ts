import { StyleSheet } from "react-native";
import { Colors } from "./colors";

export const Styles = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#444',
        borderRadius: 8,
        padding:12,
        marginBottom: 10,
        color: Colors.white,
        backgroundColor: '#222',
    },
});